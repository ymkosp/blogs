<% if signed_in? %>
	<div class="row">
		<aside class="span4">
			<section>
				<%= render 'shared/user_info' %>
			</section>
			<section>
				<%= render 'shared/stats' %>
			</section>
			<section>
				<%= render 'shared/micropost_form' %>
			</section>
		</aside>

		<div class="span8">
			<h3>User's Posts</h3>
			<%= render 'shared/feed' %>
		</div>
	</div>
<% else %>
	<div class="center hero-unit">
		<h1>Welcome to My Posts</h1>

		<h2>
			This is the home page for the
			<a href="http://malli-0370:3000/">Mallikarjuna's Blog</a>
			, a sample Rails application.
		</h2>

		<%= link_to "Sign up now!", signup_path,
			class: "btn btn-large btn-primary" %>
		</div>

		<!--%= link_to image_tag("rails.png", alt: "Rails"), 'http://rubyonrails.org/' %-->
	<% end %>
